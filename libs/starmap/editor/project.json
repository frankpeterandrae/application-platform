{
	"name": "starmap-editor",
	"$schema": "../../../node_modules/nx/schemas/project-schema.json",
	"sourceRoot": "libs/starmap/editor/src",
	"prefix": "starmap-editor",
	"projectType": "library",
	"tags": ["type:feature", "scope:starmap", "platform:browser"],
	"targets": {
		"test": {
			"executor": "@nx/vitest:test",
			"outputs": ["{options.reportsDirectory}"],
			"options": {
				"tsConfig": "libs/starmap/editor/tsconfig.spec.json",
				"vitestConfig": "libs/starmap/editor/vitest.config.mts",
				"reportsDirectory": "test-result/libs/starmap/editor/coverage",
				"passWithNoTests": true
			}
		},
		"stylelint": {
			"executor": "nx-stylelint:lint"
		},
		"typecheck": {
			"executor": "nx:noop",
			"dependsOn": ["typecheck-lib", "typecheck-test"]
		},
		"typecheck-test": {
			"executor": "nx:run-commands",
			"options": {
				"command": "npx tsc -p {projectRoot}/tsconfig.spec.json --noEmit"
			}
		},
		"typecheck-lib": {
			"executor": "nx:run-commands",
			"options": {
				"command": "npx tsc -p {projectRoot}/tsconfig.lib.json --noEmit"
			}
		}
	}
}
