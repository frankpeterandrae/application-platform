{
	"name": "paint-selector",
	"$schema": "../../../node_modules/nx/schemas/project-schema.json",
	"sourceRoot": "libs/stream-overlay/paint-selector/src",
	"projectType": "library",
	"tags": ["platform:agnostic", "scope:stream", "type:util"],
	"targets": {
		"test": {
			"executor": "@nx/vitest:test",
			"outputs": ["{options.reportsDirectory}"],
			"options": {
				"tsConfig": "libs/stream-overlay/paint-selector/tsconfig.spec.json",
				"vitestConfig": "libs/stream-overlay/paint-selector/vitest.config.mts",
				"reportsDirectory": "test-result/libs/stream-overlay/paint-selector/coverage",
				"passWithNoTests": true
			}
		},
		"typecheck": {
			"executor": "nx:noop",
			"dependsOn": ["typecheck-lib", "typecheck-test"]
		},
		"typecheck-test": {
			"executor": "nx:run-commands",
			"options": {
				"command": "npx tsc -p {projectRoot}/tsconfig.spec.json --noEmit"
			}
		},
		"typecheck-lib": {
			"executor": "nx:run-commands",
			"options": {
				"command": "npx tsc -p {projectRoot}/tsconfig.lib.json --noEmit"
			}
		}
	}
}
