# shared-node-test

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-node-test` to build the library.

## Running unit tests

Run `nx test shared-node-test` to execute the unit tests via [Jest](https://jestjs.io).
