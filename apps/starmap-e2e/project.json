{
	"name": "starmap-e2e",
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"projectType": "application",
	"sourceRoot": "apps/starmap-e2e/src",
	"implicitDependencies": ["starmap"],
	"tags": ["type:e2e", "scope:z21", "platform:browser"],
	"targets": {
		"e2e": {
			"executor": "@nx/playwright:playwright",
			"outputs": ["{workspaceRoot}/dist/.playwright/apps/starmap-e2e"],
			"options": {
				"config": "apps/starmap-e2e/playwright.config.mts"
			}
		},
		"typecheck": {
			"executor": "nx:noop",
			"dependsOn": ["typecheck-e2e"]
		},
		"typecheck-e2e": {
			"executor": "nx:run-commands",
			"options": {
				"command": "npx tsc -p {projectRoot}/tsconfig.json --noEmit"
			}
		}
	}
}
